query ChannelAboutUser($id: ID, $login: String) {
    user(id: $id, login: $login) {
        id
        description
        primaryTeam {
            id
            name
            displayName
        }
        channel {
            socialMedias {
                id
                title
                name
                url
            }
        }
    }
}