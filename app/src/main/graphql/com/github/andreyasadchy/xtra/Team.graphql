query Team($name: String!) {
    team(name: $name) {
        id
        name
        backgroundImageURL
        bannerURL
        description
        displayName
        logoURL
        owner {
            id
            login
        }
    }
}